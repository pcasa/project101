<%= form_for [current_company, @category] do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
	<%= f.collection_select :parent_id, @categories, :id, :name %>
  </p>
  <p><%= f.submit %></p>
<% end %>
